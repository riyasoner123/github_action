


    <nav class="navbar navbar-expand-lg navbar-light navigationBar">
        <div class="container-fluid">
            <a href="#" class="navbar-brand">Brand</a>
            <button type="button" class="navbar-toggler" data-bs-toggle="collapse" data-bs-target="#navbarCollapse">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarCollapse">
                <div class="navbar-nav">
                    <a href="<%= projects_path %>" class="nav-item nav-link active">Home</a>
                    <% if manager_signed_in? %>
                    <a href="<%= new_project_path %>" class="nav-item nav-link">Add new project</a>
                  <%end%>
                </div>
                <div class="navbar-nav ms-auto">
                <% unless manager_signed_in? %>
              <a href="<%= new_manager_registration_path %>" class="nav-item nav-link">Sign up</a>
                    <a href="<%= new_manager_session_path %>" class="nav-item nav-link">Sign in</a>
                   <%else%>
                   <a href="<%=profile_path%>" class="nav-item nav-link">Profile</a>
                    <a href="<%= destroy_manager_session_path %>" data-method="delete" class="nav-item nav-link">Sign out</a>
                    <%end%>
                </div>
            </div>
        </div>
    </nav>
